// Aster Cafe, a node inside the downtown_minneapolis bbox (verified at the
// reference server). No {{bbox}} substitution needed; the id is static.
[out:json][timeout:25];
node(id:1077152668);
out body;
