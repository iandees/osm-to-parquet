// timeline of way 69963816: its own version (4) did not change inside the
// history window, but a member node moved, producing a minor version; no
// bbox needed.
[out:json][timeout:25];
timeline(way,69963816);
out;
