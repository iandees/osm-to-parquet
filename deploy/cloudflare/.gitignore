node_modules/
.wrangler/

# Generated by `npm run cf-typegen` (`wrangler types`) from wrangler.jsonc
# plus the vendored workerd runtime types; regenerate rather than commit,
# since it goes stale the moment either changes. Needs the repo-root
# Dockerfile (workstream W4) to exist for `containers` validation to pass
# — see README.md.
worker-configuration.d.ts

# vitest / coverage output
coverage/
